class ListView extends Backbone.View {
	
    constructor(options) {
        options = options || {};

        _.defaults(options, {
            className: 'comp-layers-list'
        });

        super(options);
    }

    initialize() {

    }

    render() {

    }
}
